<h1>Account Layout</h1>

{{!-- <ul class="listCategory">
    {{#each account}}
    <li>{{this.name}}</li>
    {{/each}}
</ul> --}}

<button><a href="./account/create">Create</a></button>
<a href="./account/create.hbs">Update</a>
<a href="./account/create.hbs">Delete</a>

<div class="mt-4">
    <div class="row">
        {{#each account}}
        <div class="col-sm-6 col-lg-2">
            <div class="card">
                <a href="/account/{{this.slug}}">
                    </a>
                    <h>Slug la: {{this.slug}}</h>
                    <div class="card-body">
                        <a href="/account/{{this.slug}}">
                            <h5 class="card-title">{{this.name}}</h5>
                        </a>
                        <a href="/account/{{this.slug}}" class="btn btn-primary"> Account Detail </a>
                    </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>