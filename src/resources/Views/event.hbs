<h1 class="text-center">Event </h1>
<div class= "container-fluid">
    <div class="mt-4">
        <ul class="row">
            <div class="col-md-3">
                <ul class="list-group">
                    <h4>Event List</h4>
                    {{#each event}}
                        <a class="list-group-item list-group-item-action list-hover" href="/event/{{this.slug}}"><li>{{this.name}}</li></a>
                    {{/each}}
                </ul>
            </div>
            <div class="col-md-9">
                <div class="row">
                {{#each event}}
                <div class="card-group mb-4 col-12 col-sm-6 col-md-4">
                    <div class="card card-event-page">
                        <a href="/event/{{this.slug}}">
                            <img class="card-img-news" src="{{this.image}}" alt="{{this.name}}">
                        </a>
                        <div class="card-body">
                            <a href="/event/{{this.slug}}">
                                <h5 class="card-title title-event ">{{this.name}}</h5>
                            </a>
                            <p class="card-text test1 overflow-hidden">{{this.description}}</p>
                        </div>
                        <a class="card-footer btn-detail" href="/event/{{this.slug}}">Detail</a>
                    </div>
                </div>
                {{/each}}
                </div>
            </div>
        </ul>
    </div>
</div>